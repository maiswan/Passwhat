<Window x:Class="Maiswan.Passwhat.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p="clr-namespace:Maiswan.Passwhat.WpfApp.Properties"
        xmlns:local="clr-namespace:Maiswan.Passwhat.WpfApp" d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.AppTitle}" Height="350" Width="600"
        Closing="Window_Closing">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4,0,2"/>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="C" Command="{Binding CopyCommand}"/>
        <KeyBinding Modifiers="Ctrl" Key="N" Command="{Binding RefreshCommand}"/>
    </Window.InputBindings>

    <Grid Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="24,12,24,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="*" MinWidth="64" MaxWidth="128"/>
                    <ColumnDefinition Width="*" MinWidth="64" MaxWidth="128"/>
                </Grid.ColumnDefinitions>
                <TextBox Padding="4"
                 Text="{Binding Password}"
                 IsReadOnly="True" IsReadOnlyCaretVisible="True"
                 FontSize="14" FontFamily="Cascadia Code,Courier New"/>

                <Button Grid.Column="1" Content="{x:Static p:Resources.Copy}" Padding="8,0" Margin="2,0" CommandParameter="{Binding Password}" Command="{Binding CopyCommand}"/>
                <Button Grid.Column="2" Content="{x:Static p:Resources.New}" Padding="8,0" Command="{Binding RefreshCommand}"/>
            </Grid>

            <TextBlock Margin="0,4" Text="{x:Static p:Resources.PasswordWeak}" Foreground="DarkRed">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsPasswordConfigWeak}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                        <Setter Property="Visibility" Value="Hidden"/>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="24,0,24,8" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="{x:Static p:Resources.Options}" FontWeight="SemiBold"/>
                <Border BorderThickness="1" BorderBrush="LightGray" Margin="0,4" Padding="16,8">
                    <StackPanel>
                        <TextBlock Text="{x:Static p:Resources.Length}"/>
                        <DockPanel Margin="0,4">
                            <TextBox Margin="0,0,16,0" Padding="4" Width="48" Text="{Binding Length, UpdateSourceTrigger=PropertyChanged}"/>
                            <Slider Minimum="{x:Static local:MainViewModel.MinLength}" Maximum="{x:Static local:MainViewModel.MaxLength}"
                            Value="{Binding Length, UpdateSourceTrigger=PropertyChanged}"
                            VerticalAlignment="Center"/>
                        </DockPanel>

                        <CheckBox Content="{x:Static p:Resources.Latin}" IsChecked="{Binding IsLatinEnabled}"/>
                        <CheckBox Content="{x:Static p:Resources.Digit}" IsChecked="{Binding IsDigitEnabled}"/>
                        <CheckBox Content="{x:Static p:Resources.Symbol}" IsChecked="{Binding IsSymbolEnabled}"/>
                        <DockPanel>
                            <CheckBox Content="{x:Static p:Resources.CustomSet}" IsChecked="{Binding IsCustomSetEnabled}"/>
                            <TextBox Margin="16,2,0,2" Padding="2,0" Text="{Binding CustomSet, UpdateSourceTrigger=PropertyChanged}"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <TextBlock Text="{x:Static p:Resources.CopiedPasswords}" FontWeight="SemiBold" Margin="0,8,0,0"/>
                <Border BorderThickness="1" BorderBrush="LightGray" Margin="0,4" Padding="16,8">
                    <Grid>
                        <TextBlock Text="{x:Static p:Resources.NoCopiedPasswords}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CopiedPasswords.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ItemsControl ItemsSource="{Binding CopiedPasswords}" BorderThickness="0" Grid.IsSharedSizeScope="True" IsTabStop="False">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="4*"/>
                                            <ColumnDefinition Width="*" MinWidth="64" MaxWidth="128"/>
                                            <ColumnDefinition Width="*" MinWidth="64" MaxWidth="128"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding}" FontFamily="Cascadia Code,Courier New" Margin="0,0,24,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                                        <Button Grid.Column="1" Command="{Binding DataContext.CopyCommand, ElementName=Root}" CommandParameter="{Binding}" Content="{x:Static p:Resources.Copy}" Padding="8,2" Margin="2,1,0,1"/>
                                        <Button Grid.Column="2" Command="{Binding DataContext.RemoveCommand, ElementName=Root}" CommandParameter="{Binding}" Content="{x:Static p:Resources.Remove}" Padding="8,2" Margin="2,1,0,1"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <StatusBar Grid.Row="2" Padding="24,8">
            <StatusBarItem Padding="0">
                <TextBlock>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding LatestAction.Action}" Value="{x:Static local:LogAction.Copy}">
                                    <Setter Property="Text" Value="{Binding LatestAction.Password, StringFormat={x:Static p:Resources.CopiedPasswordLog}}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding LatestAction.Action}" Value="{x:Static local:LogAction.Generation}">
                                    <Setter Property="Text" Value="{Binding LatestAction.Password, StringFormat={x:Static p:Resources.GeneratedPasswordLog}}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding LatestAction.Action}" Value="{x:Static local:LogAction.Removal}">
                                    <Setter Property="Text" Value="{Binding LatestAction.Password, StringFormat={x:Static p:Resources.RemovedPasswordLog}}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right" Padding="0" Content="{Binding Version}" ContentStringFormat="{x:Static p:Resources.Build}"/>
        </StatusBar>
    </Grid>
</Window>
